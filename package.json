{
  "name": "freechat-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "predeploy": "npm run build:protocol",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply freechat-db --local",
    "db:remote": "wrangler d1 migrations apply freechat-db --remote",
    "build:protocol": "clang --target=wasm32 -Oz -nostdlib -Wl,--no-entry -Wl,--export-memory -Wl,--export=fc_header_size -Wl,--export=fc_meta_size -Wl,--export=fc_scratch_ptr -Wl,--export=fc_frame_len -Wl,--export=fc_encode -Wl,--export=fc_decode -o src/protocol/freechat_protocol.wasm src/protocol/freechat_protocol.c",
    "check": "node --check src/index.js && node --check src/app/router.js && node --check src/realtime/ChatRoom.js && node --check src/protocol/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
